# Legacy note: the canonical Codex project config for this repo lives in
# `.codex/config.toml`.
#
# Keep this file only as a compatibility placeholder for older notes or tools
# that still expect a root-level `config.toml`.
